# FuelEU Frontend

Install:
  cd frontend
  npm install

Run dev:
  npm run dev

Build:
  npm run build

Notes:
- Frontend expects backend at http://localhost:4000 (change VITE_API_BASE env)
- Tailwind is preconfigured. If you don't have Tailwind installed globally, the dev scripts handle it.
